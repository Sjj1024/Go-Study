package main
import "fmt"

func main() {
	// \t表示回车，会覆盖行首的内容，推荐使用行注释。源代码也是行注释
	// 导入无用的包或者定义无用的变量，不使用的话，编译都不会通过， 会报错
	// 运算符两边加上空格
	// 代码风格：{前面的大括号要和函数在一行，不然报错}
	/*
	块注释内容
	*/
	fmt.Println("wo\tai\tni\tzhogn\tguo")
}
